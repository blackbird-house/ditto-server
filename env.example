# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=3000

# Database Configuration
# For development: SQLite (file-based, no setup required)
DATABASE_URL=./data/ditto-dev.db

# For production: MongoDB or PostgreSQL
# DATABASE_URL=mongodb://localhost:27017/ditto-prod
# DATABASE_URL=postgresql://user:password@localhost:5432/ditto_prod

# CORS Configuration (comma-separated list of allowed origins)
# CORS_ORIGIN=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# API Keys (add as needed)
# API_KEY=your-api-key-here
# JWT_SECRET=your-jwt-secret-here

# API Secret (required for all requests)
# Development: dev-secret-key-12345
# Test: test-secret-key-67890
# Staging: staging-secret-key-abcdef
# Production: Use cryptographically secure 64-char hex string
API_SECRET=dev-secret-key-12345

# JWT Secret (for token signing)
# Production: Use cryptographically secure 64-char hex string
JWT_SECRET=your-jwt-secret-here

# External Services (add as needed)
# REDIS_URL=redis://localhost:6379
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-password
